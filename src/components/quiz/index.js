/**
 * Módulo quiz — fluxo guiado reutilizável (a home continua a usar `CurationQuizSection` → `HomeQuiz`).
 *
 * import HomeQuiz from "./components/quiz/HomeQuiz.jsx";
 * import { Quiz } from "./components/quiz";
 */

export { default, Quiz } from "./HomeQuiz.jsx";
