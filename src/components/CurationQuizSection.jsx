/**
 * Secção “Escolha pelo resultado” na home (`id="curadoria"`).
 * Implementação canónica do fluxo guiado: `./quiz/HomeQuiz.jsx`.
 *
 * Para evoluir o quiz independentemente da copy da secção na home,
 * edite `quiz/HomeQuiz.jsx`. Para usar noutras páginas:
 * `import HomeQuiz from "./quiz/HomeQuiz.jsx"` ou `import { Quiz } from "./quiz"`.
 */
export { default } from "./quiz/HomeQuiz.jsx";
